package game.scene 
{
	import display.scene.SceneViewBase;
	import render2d.core.gl.Driver;
	
	public class GameSceneView extends SceneViewBase 
	{
		
		public function GameSceneView(driver:Driver) 
		{
			super(driver);
		}
	}
}